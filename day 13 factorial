//factorial of a given number
#include<stdio.h>
main()
{
	 int n,fact=1;
	 printf("enter a number\n");
	 scanf("%d",&n);
	 while(fact<=n)
	 {
	 	if(n%fact==0)
	 	{
	 		printf("%d\n",fact);
		}
		fact++;
	 }
}
