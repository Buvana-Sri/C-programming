//sum of array elements
#include<stdio.h>
main()
{
	 int i,j,n,sum;
	 scanf("%d",&n);
	 int a[n];
	 for(i=0;i<n;i++)
	 scanf("%d",&a[i]);
	 sum=0;
	 for(i=0;i<n;i++)
	 {
	 	 sum=sum+a[i];
	 }
	 	 printf("%d ",sum);
}
